import '../../../../routes/routes_helper.dart';

class MeasurementAndDetails extends StatelessWidget {
  const MeasurementAndDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
